using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace KnapSack.Utilities
{
    public static class RequestUtilities
    {
        // TODO what is this lmfao
        // takes a string, a model and a property name. returns a function of type <T, bool> to use with
        // linq requests.
        /*public static Func<T, bool> GenerateExpressionFromRequestArguments<T> (T model, string modelProperty, string requestArguments)
        {
            requestArguments.Split(',');

            model.GetType()
        }*/
    }
}